# Session Learnings

> Persistent knowledge extracted from AI coding sessions.
> Captures decisions, context, action items, and insights that should survive between sessions.
> Add entries with `/capture-session` (full sweep) or `/capture-learning` (single item).

## Decisions

- **[2026-05-06]** Gather disruption should be computed only from already-selected significant event windows for now, not from arbitrary gather-rate drops. Rationale: broader drops can include self-caused economic transitions such as building production buildings, farms, or moving between resources; the current goal is raid/encounter-caused economic impact. *(source: conversation)*
- **[2026-05-06]** In Event impact → Encounter losses, gather disruption is a lower loss-list item next to the concrete lost item (for example `Yatai`) and is folded into `Immediate loss`/`Total loss`; it is not a separate summary row. The explanation belongs on the lower-row tooltip, not as inline summary prose. *(source: conversation/code verification)*
- **[2026-05-06]** In `Opportunity lost: resources lost by selected time`, gather disruption is a cumulative resource component equal to the sum of event-window gather disruption at or before the selected timestamp. It contributes to the Opportunity Lost total; `TC idle seconds` remains a separate production/underproduction diagnostic and does not contribute to resource-loss total. *(source: conversation/code verification)*
- **[2026-05-04]** Rendered-report cache version is derived as `${baseline}-${buildIdHash}-${analyticsHash}` in `apps/web/src/lib/renderedReportCache.ts`, where the build hash comes from `VERCEL_GIT_COMMIT_SHA` (with `AOE4_RENDERED_REPORT_CACHE_BUILD_ID`/`GIT_COMMIT_SHA` fallbacks) and the analytics hash covers `NEXT_PUBLIC_POSTHOG_TOKEN`/`HOST`/`ENVIRONMENT`. Manual `CACHE_BASELINE` ('v8') is kept for grep-able history; deploys and analytics-env rotations now auto-invalidate the persistent cache instead of waiting up to 24 h. *(source: conversation/code review)*
- **[2026-05-04]** Match-outcome lowercasing belongs on the view model as `header.outcomeLowercase`, not as `.toLowerCase()` in the formatter. Analytics callers and HTML callers consume the lowercased copy directly so the formatter never makes UI casing choices the view model could pre-compute. *(source: conversation/code review)*
- **[2026-05-04]** Reddit feedback URL is the single constant `REDDIT_FEEDBACK_HREF` exported from `packages/aoe4-core/src/formatters/sharedFormatters.ts`. Was previously duplicated in `homePageHtml.ts` and `postMatchHtml.ts`. *(source: conversation/code review)*
- **[2026-05-04]** Sample-report identity is centralized in `apps/web/src/lib/sampleMatch.ts` as `SAMPLE_MATCH = { profileSlug: '8139502', gameId: 229727104, sig, selectedTimeSeconds: 1191 }`; both the home-page `sampleMatchHref` and the analytics click-handler import it so they cannot drift. *(source: conversation/code review)*
- **[2026-05-04]** `OpportunityLostComponents` keys are camelCase (`villagersLost`, `underproduction`, `lowUnderproduction`); the matching DOM `data-opportunity-lost-component` value is kebab-case (`low-underproduction`). Was previously `low_underproduction` snake_case. *(source: conversation/code review)*
- **[2026-05-04]** Canonical population field is `TimeSeriesResources.population`; the `totalPopulation`/`totalPop`/`pop` fallbacks were dropped from both the parser and `villagerOpportunity.collectTotalPopulationPoints`. *(source: conversation/code review)*
- **[2026-05-04]** Significant-event marker color in the allocation chart is the *beneficiary* of the loss event (player who inflicted the damage), not the victim. Documented in `significantEventMarkerColor` so a future refactor can't quietly flip it back. *(source: conversation/code review)*
- **[2026-05-04]** PostHog event names use spaces (`"match viewed"`, `"band filter changed"`, `"home sample report opened"`) — locked-in convention. Renaming any is a breaking change for analytics dashboards; documented at the top of `posthogAnalytics.ts`. *(source: conversation/code review)*
- **[2026-05-04]** Local development PostHog analytics are intentionally enabled rather than suppressed; local events should be labeled with `app_environment=local`, `is_local_environment=true`, and `app_hostname=localhost` so they can be queried and filtered separately. *(source: conversation/code verification)*
- **[2026-05-04]** Analytics may record `has_sig` and stable match identity, but must never send raw, encoded, or nested `sig` query params in event properties, current URLs, referrers, or outbound/link metadata. *(source: conversation/code verification)*
- **[2026-05-03]** Browser-facing `Cache-Control` on the home and match routes (`apps/web/src/app/route.ts`, `apps/web/src/app/matches/[profileSlug]/[gameId]/route.ts`) is `max-age=0, must-revalidate` (or `private, max-age=0, must-revalidate` for signed match URLs). Long TTLs are moved to a separate `CDN-Cache-Control` header so the Vercel edge stays warm while browsers always revalidate after a deploy. Vercel auto-purges the edge on each deployment. *(source: conversation/code verification)*
- **[2026-05-03]** In `NODE_ENV === 'development'`, the home and match routes return `cache-control: no-store` and omit `CDN-Cache-Control`. Gate is on `=== 'development'` (not `!== 'production'`) so jest's `NODE_ENV=test` path still asserts production headers. *(source: conversation/code verification)*
- **[2026-05-03]** Signed (private) match URLs skip CDN caching entirely (no `CDN-Cache-Control` header) since the response is already marked `private`. *(source: conversation/code verification)*
- **[2026-05-03]** Player color identity in post-match charts must come from `PostMatchPlayerDisplay.color`; dash style remains the perspective encoding (`you` solid, `opponent` dashed). This prevents mismatches when the viewed player is player 2 and therefore has the player-2 color. *(source: conversation/code verification)*
- **[2026-05-02]** Post-match `militaryActive` should match AoE4World army value semantics: live units use base resource cost, not tier-scaled replacement value. Upgrade/tier scaling belongs in separate combat-adjusted views, not the raw army-value hover number. *(source: conversation/code verification)*
- **[2026-05-02]** Unit lifecycle accounting must operate across a unit line, not only per resolved build-order row; upgraded-row deaths should consume active lower-tier units with the same `baseId`/line key when the upgraded row has no active count. *(source: conversation/code verification)*
- **[2026-05-02]** Destroyed-only upgraded unit rows must remain in `ResolvedBuildOrder.resolved`; dropping them loses AoE4World death events such as Veteran Spearman/Sipahi deaths after an upgrade. *(source: conversation/code verification)*
- **[2026-05-02]** Religious units such as Imam and Monk count as `militaryActive` army value for AoE4World alignment, while religious buildings such as monasteries remain economic infrastructure. *(source: conversation/code verification)*
- **[2026-05-01]** Yatai unit `pbgid=9001316` uses AoE4World `unknown["14"]` as production and `unknown["15"]` as destruction/removal; counting only bucket `14` creates impossible active Yatai counts in live match `229727104`. *(source: conversation/code verification)*
- **[2026-05-01]** Yatai Trade Cart `pbgid=9003449` is a zero-cost delivery/output artifact, not deployed capital; its `unknown["15"]` bucket should remain ignored and it should not appear in deployed-resource breakdowns. *(source: conversation/code verification)*
- **[2026-05-01]** Keep root analyzer and `apps/web` resolver/mapping copies in sync for unknown-bucket handling so static reports and the deployed Next app use the same accounting semantics. *(source: conversation/code verification)*
- **[2026-05-01]** Use `apps/web` as the standalone Vercel web app boundary. The root/static report remains the staging/reference area, and the deployable app should copy needed logic rather than directly importing root analyzer runtime code. *(source: conversation)*
- **[2026-05-01]** Replace separate "Deployed resource pool over time" and "Strategic allocation state" sections with one "Allocation lead and mix over time" widget because it captures the same strategic signal more simply. *(source: conversation)*
- **[2026-05-01]** Remap deployed-resource bands into larger categories: Economic, Technology, Military, and Other, so the widget is easier to scan while preserving band-level inspector detail. *(source: conversation)*
- **[2026-05-01]** Allocation category mapping is: Economic band to Economic; population cap to Other; military buildings, military active, and defensive to Military; research and advancement to Technology. *(source: conversation)*
- **[2026-05-01]** Allocation charts use mixed encoding: Economic, Technology, and Military show percentage share, while Overall shows absolute deployed resource value. Share is best for strategic mix; Overall should preserve total deployed-resource scale. *(source: conversation)*
- **[2026-05-01]** Hover inspector keeps original band rows and composition behavior, with added collapsible category headers, preserving detail access while reducing visual overload. *(source: conversation)*
- **[2026-05-01]** Vercel production project is named `aoe4-game-analyzer-web`, with explicit `apps/web/vercel.json` to force Next.js deployment config because the first project config defaulted to static output and failed deployment. *(source: conversation)*
- **[2026-05-01]** Float should mean literal live stockpile only, from resource series food/wood/gold/stone/olive oil. Inferred residual mixes real float with model gaps and normalization artifacts, so it should not be labeled as float. *(source: conversation)*
- **[2026-05-01]** Same-timestamp production/destruction must remain separate in item deltas, even though active pool value nets to zero, because destroyed accounting needs the negative event. *(source: conversation)*
- **[2026-05-01]** Reconciliation gaps should be exposed separately from Float; model-health diagnostics should not be presented as player strategy allocation. *(source: conversation)*

## Action Items

- [ ] **[2026-05-06]** Add gather disruption to the villager opportunity section later as a P1 feature; do not include it there yet. — Owner: engineering
- [ ] **[2026-05-04]** Stop hard-coding the PostHog write key as a default in `apps/web/src/lib/posthogAnalytics.ts`. Already captured as P1 in `NEXT_STEPS.md` with full migration steps (flip `DEFAULT_POSTHOG_TOKEN` to `''`, update `posthogAnalytics.test.ts`, confirm Vercel preview/production env wiring). — Owner: engineering
- [ ] **[2026-05-04]** Extract a single rule list for analytics sanitization (`stripSensitiveQueryParams`, `shouldDropAnalyticsProperty`) so the server-side TypeScript and the inline-client JS-as-string implementations in `posthogAnalytics.ts` can't drift. Skipped this round to keep the review-fix scope small; flagged in code comment. — Owner: engineering
- [ ] **[2026-05-04]** Validate the `villagerOpportunity` changes (TC-busy prefix sums, population-cap lock at 200, transformed-villager expander) against real Jeanne d'Arc and Ottoman games, per the CLAUDE.md learning about validating against real game output rather than only synthetic fixtures. — Owner: engineering
- [ ] **[2026-05-04]** Generate and verify the remaining less-common analytics events locally, including invalid match URL submission, outbound link clicks, mobile timeline changes, and event explanation opens. — Owner: engineering
- [ ] **[2026-05-04]** Build saved PostHog dashboard/insight views for the root-page funnel, match-page interactions, engagement summaries, acquisition sources, and signature-leak guard queries. — Owner: engineering
- [ ] **[2026-05-02]** Consider adding a durable real-match regression fixture for RepleteCactus game `231277359` so the exact AoE4World `17:40` army-value case is covered in addition to the synthetic upgraded-death fixture. — Owner: engineering
- [ ] **[2026-05-02]** Review non-hover consumers such as `armyReconstruction` and `significantResourceLossEvents` for the same cross-tier death and tier-multiplier assumptions if they are expected to match raw AoE4World army value. — Owner: engineering
- [ ] **[2026-05-01]** Add `@LEARNINGS.md` to `CLAUDE.md` if Claude sessions should auto-load these learnings. — Owner: project maintainer
- [ ] **[2026-05-01]** Resolve `npm audit` findings for `next`/`postcss`; current fix path suggests a breaking Next 16 upgrade. — Owner: project maintainer
- [ ] **[2026-05-01]** Decide whether to delete the empty Vercel project named `web`; user said "never mind," so it remains. — Owner: user
- [ ] **[2026-05-01]** Commit and push the deployment config addition `apps/web/vercel.json` and any current web/static changes. — Owner: project maintainer
- [ ] **[2026-05-01]** Consider adding caching for AoE4World/API fetches in `apps/web`. — Owner: project maintainer
- [ ] **[2026-05-01]** Consider setting Vercel project settings explicitly in dashboard/API for framework/Node version instead of relying only on `vercel.json`. — Owner: project maintainer
- [ ] **[2026-05-01]** Add a diagnostic reconciliation row or section: `raw gathered - live stockpile - modeled gross costs`, ideally with per-resource breakdown. — Owner: engineering
- [ ] **[2026-05-01]** Revisit `buildCumulativeGatheredSeries` scaling for timestamp-level accounting; consider raw vs scaled gathered series. — Owner: engineering
- [ ] **[2026-05-01]** Continue/finish separate Yatai and unknown-bucket work already in progress. — Owner: engineering

## Context

- **[2026-05-06]** Example URL for gather-disruption verification: `/matches/8139502/229727104?sig=...&t=495` at `8:15`. Current hover payload shows columns as Sengoku Daimyo then Macedonian Dynasty; Sengoku opportunity lost is `246` (`41` underproduction + `205` gather disruption), Macedonian opportunity lost is `105` (`105` underproduction + `0` gather disruption), and TC idle seconds are `23s` / `11s`. Earlier timestamps before the event, such as around `7:20`, show gather disruption `0`. *(source: browser/local hover-data verification)*
- **[2026-05-06]** The 205-resource gather disruption for the `8:15` example is attached to the same side as the `Yatai` economic loss in the Encounter losses list. Future discussion should check party/column perspective carefully before saying the impacted party is Macedonian or Sengoku. *(source: conversation/browser verification)*
- **[2026-05-04]** Claude's Edit/Write tools (and any tool that takes file content via JSON parameters) interpret `\0` JSON escapes literally. Writing `'\0'` as a TypeScript string literal can land a real NUL byte in the file, which makes git/`file(1)`/`diff` treat the source as binary. Workaround: declare `const X = String.fromCharCode(0)` and reference the constant; or build bytes through Bash heredocs that don't go through JSON parsing. Discovered when `git diff` reported `apps/web/src/lib/renderedReportCache.ts` as `Binary files differ` after an Edit-tool round-trip. *(source: conversation/tool verification)*
- **[2026-05-04]** `cumulativeBusyTownCenterSecondsAt` in `villagerOpportunity.ts` is now `O(log N + bounded straddle scan)` per call via `buildVillagerTrainingPrefixSums`. Straddle scan is bounded by the longest training time (~30 s) so the linear part is small in practice. Original implementation was `O(N)` per call inside the `O(T)` timeline loop = `O(NT)` total. *(source: conversation/code review)*
- **[2026-05-04]** `.gitignore` excludes `.playwright-mcp/`, `home-*.png`/`home-snapshot.yml`, and `report-*.png`/`report-snapshot.yml`. UI screenshots and Playwright MCP debug artifacts had been accumulating untracked in the working tree. *(source: conversation/code review)*
- **[2026-05-04]** PostHog MCP was available through `codex mcp list` with OAuth auth, and live localhost events were queried through the MCP during verification. *(source: conversation/tool verification)*
- **[2026-05-04]** Browser analytics bootstrap and sanitization live in `apps/web/src/lib/posthogAnalytics.ts`; root and match pages use it for manual product events with PostHog autocapture disabled and session recording disabled. *(source: conversation/code verification)*
- **[2026-05-04]** Verified local PostHog events included `$pageview`, `home sample report opened`, `match viewed`, `timestamp selected`, `allocation category toggled`, `band filter changed`, `home engagement summary`, and `match engagement summary`. *(source: conversation/tool verification)*
- **[2026-05-04]** The verified sample match identity was `game_id=229727104` and `profile_slug=8139502`; match events carried those identifiers where expected. *(source: conversation/tool verification)*
- **[2026-05-03]** Vercel honors a separate `CDN-Cache-Control` header that overrides `Cache-Control` only at the edge, allowing browser and edge TTLs to diverge. *(source: conversation/code verification)*
- **[2026-05-03]** Vercel auto-purges its edge CDN cache for SSR/route-handler responses on each new deployment, so an `s-maxage` set via `CDN-Cache-Control` is at most one deploy old. *(source: conversation)*
- **[2026-05-03]** `apps/web/src/lib/renderedReportCache.ts` already keys server-side `unstable_cache` on `VERCEL_GIT_COMMIT_SHA` (plus an analytics-config hash and a `CACHE_BASELINE` constant), so the persistent rendered-HTML cache auto-rotates on deploy without manual revalidation. The 5-minute in-memory LRU is wiped on cold start, which deploys force. *(source: conversation/code verification)*
- **[2026-05-03]** The `/` and `/matches/[profileSlug]/[gameId]` routes are pure HTML route handlers with inline scripts — no Next.js client chunks for these pages — so HTML-level caching is the entire freshness concern; there are no fingerprinted JS bundles to keep in sync. *(source: conversation/code verification)*
- **[2026-05-03]** jest runs with `NODE_ENV=test` by default; gating dev-only behavior on `=== 'development'` (rather than `!== 'production'`) keeps test assertions stable. *(source: conversation/code verification)*
- **[2026-05-03]** Regression coverage for the allocation legend/line identity bug uses a player-2 perspective fixture where RepleteCactus/Ottomans is red and `sohaijim2022`/Golden Horde is blue; unit, integration, and route e2e tests assert the legend, leader strip, allocation paths, and age markers all use the same player-color mapping. *(source: conversation/code verification)*
- **[2026-05-02]** In RepleteCactus/Ottomans game `231277359`, AoE4World army value at `17:40` (`t=1060`) is `1485`; after the fix, the web hover model reports `Mehter=720`, `Spearman=400`, `Imam=300`, and `Scout=65`. *(source: conversation/code verification)*
- **[2026-05-02]** The discrepancy for game `231277359` was decomposed as: Hardened/Sipahi tier multipliers inflated live value, Veteran destroyed-only rows were dropped or could not subtract lower-tier active units, and Imams were excluded from `militaryActive`. *(source: conversation/code verification)*
- **[2026-05-02]** Regression coverage for upgraded-unit army value now spans unit tests (`resourcePool`, `buildOrderResolver`), post-match integration, and CLI e2e via `__tests__/helpers/upgradedUnitDeathsFixture.ts`. *(source: conversation/code verification)*
- **[2026-05-02]** Verification commands for commit `46ea161 Fix upgraded unit army value accounting` were root `npm run build`, root `npm test`, `npm --workspace aoe4-game-analyzer-web run verify`, and `npm --workspace aoe4-game-analyzer-web run build`. *(source: conversation/code verification)*
- **[2026-05-01]** In live cached match `tmp/229727104-live.summary.json`, Sengoku Yatai resolves to 9 production timestamps and 7 destruction/removal timestamps; at `t=1362`, the correct active Yatai count is `3` with value `375`. *(source: conversation/code verification)*
- **[2026-05-01]** Verification commands used for the Yatai/Yatai Trade Cart accounting change were root `npm test -- --runInBand`, root `npm run build`, and `cd apps/web && npm run verify`. *(source: conversation/code verification)*
- **[2026-05-01]** Production app URL is `https://aoe4-game-analyzer-web.vercel.app`. *(source: conversation)*
- **[2026-05-01]** Vercel account/scope used for deployment is `benshoemakerxyz-3472s-projects`. *(source: conversation)*
- **[2026-05-01]** `vercel project add` created `aoe4-game-analyzer-web` with "Other" framework/static output defaults; adding `apps/web/vercel.json` made production deployment work. *(source: conversation)*
- **[2026-05-01]** `vercel ls --yes` unexpectedly auto-linked/created local Vercel metadata and an empty project named `web`. *(source: conversation)*
- **[2026-05-01]** `apps/web/.gitignore` now includes `.vercel` and `.env*.local`; Vercel link created local ignored `.vercel` metadata and `.env.local`. *(source: conversation)*
- **[2026-05-01]** Production home page was smoke-tested with `curl` and returned `200`; private `sig` match route was not production-smoke-tested to avoid transmitting a private sig without explicit confirmation. *(source: conversation)*
- **[2026-05-01]** In Beasty/Sengoku match `229727104`, at `21:09`, raw gathered was `33,471`, visible stockpile was `1,685`, modeled active + destroyed costs after the same-timestamp fix were `31,546`, leaving only `240` true raw ledger gap. *(source: conversation)*
- **[2026-05-01]** The midgame gathered-series scaling artifact in the Beasty/Sengoku `21:09` example was `+1,381`: model gathered `34,852` versus raw gathered sample `33,471`. *(source: conversation)*
- **[2026-05-01]** The previous roughly `3,700` unexplained resources decomposed into Yatai cost, gathered-series scaling, same-timestamp delta netting, and a small raw ledger mismatch. *(source: conversation)*
- **[2026-05-01]** The same-timestamp issue affected three Mounted Samurai in the Beasty/Sengoku example: `3 * 324 = 972`. *(source: conversation)*

## Bugs & Issues

- **[2026-05-06]** Gather disruption initially appeared as a separate Event impact summary row and had the tooltip in the wrong location/size. Fixed by removing the separate summary row, keeping gather disruption in the lower Encounter losses list with a compact tooltip, and folding its value into `Immediate loss`/`Total loss`. *(packages/aoe4-core/src/formatters/postMatchHtml.ts, packages/aoe4-core/src/formatters/postMatchInteractionScript.ts) (source: conversation/code verification)*
- **[2026-05-06]** Opportunity Lost selected-time accounting initially omitted gather disruption, so event-window economic impact did not reconcile with the Opportunity Lost total. Fixed by adding `opportunityLostComponents.gatherDisruption`, accumulating event-window values at or before each hover timestamp, updating the client hover script, and bumping rendered-report cache baseline to `v13`. *(packages/aoe4-core/src/formatters/postMatchHtml.ts, packages/aoe4-core/src/presentation/postMatchPresentation.ts, apps/web/src/lib/renderedReportCache.ts) (source: conversation/code verification)*
- **[2026-05-04]** Match-route `errorDocument` was using `message.replace(/[<>&]/g, '')` (strip-instead-of-escape) to render error messages. Fixed in commit `76f6ef2` by switching to `escapeHtml` from `sharedFormatters`; regression test asserts an injected `bad <game> id` survives as `bad &lt;game&gt; id`. *(apps/web/src/app/matches/[profileSlug]/[gameId]/route.ts) (source: conversation/code review)*
- **[2026-05-04]** `expandTransformedJeanneItems` in `resourcePool.ts` gated villager→military expansion on the literal substring `"jeanne"` in the item haystack, which would silently miss future villager-to-military transforms. Fixed in commit `76f6ef2` by renaming to `expandTransformedVillagerItems` and dropping the substring check; detection now relies only on the build-order entry having a non-empty `transformed` array. *(source: conversation/code review)*
- **[2026-05-04]** `setInterval` (stage advancement) and `setTimeout` (long-wait message) in the match-loading interstitial were never cleared. Fixed in commit `76f6ef2` by tracking both timer ids and calling `clearTimers()` in `navigateToReport` before `location.replace`. *(apps/web/src/lib/matchLoadingPageHtml.ts) (source: conversation/code review)*
- **[2026-05-04]** Persistent rendered-report cache key did not change with PostHog token/host/environment, so a token rotation could leave cached HTML serving the old token for up to 24 h. Fixed in commit `76f6ef2` by folding a hash of the analytics-env triple plus a build SHA into `CACHE_VERSION`. *(apps/web/src/lib/renderedReportCache.ts) (source: conversation/code review)*
- **[2026-05-04]** `apps/web/src/lib/renderedReportCache.ts` momentarily became binary after an Edit-tool round-trip embedded a literal NUL byte in the source. Fixed by replacing the affected `'\0'` literal with `const MEMORY_CACHE_KEY_SEPARATOR = String.fromCharCode(0)` and switching the `readAnalyticsIdentity` join separator to `'|'` (it only needs to be unambiguous for hashing). Verified by `file ...renderedReportCache.ts` reporting `Unicode text, UTF-8 text` and `git diff --cached` rendering as text, not `Binary files differ`. *(source: conversation/tool verification)*
- **[2026-05-04]** Initial local analytics testing found local events were not reliably queryable and engagement summaries could lag or batch too late. Fixed by making local delivery explicit, disabling request batching for the browser bootstrap, and emitting summary events on visibility/page lifecycle transitions. *(source: conversation/code verification)*
- **[2026-05-04]** Early PostHog leak checks exposed that encoded or nested signature query params could still appear in URL/referrer-like properties. Fixed by centralizing property sanitization and adding tests that reject raw, encoded, and nested `sig` values. *(source: conversation/code verification)*
- **[2026-05-03]** Local dev refreshes weren't picking up source changes because the home and match route handlers set `Cache-Control: public, max-age=300, ...` even in dev. Chrome's "hard reload" bypasses cache for the document URL only — not for stale-while-revalidate background serve or BFCache — so changes appeared cached even after a hard reload. Fixed in commit `00697be` by gating cache headers on `NODE_ENV`. *(source: conversation/code verification)*
- **[2026-05-03]** Returning production visitors could see stale HTML for up to 5 min (max-age) plus up to 1 week of stale-while-revalidate background serve after a deploy, because the previous match-route header was `public, max-age=300, s-maxage=86400, stale-while-revalidate=604800`. Fixed in commit `00697be` by switching browser-facing `Cache-Control` to `max-age=0, must-revalidate` and moving long TTLs to `CDN-Cache-Control`. *(source: conversation/code verification)*
- **[2026-05-03]** `Allocation lead and mix over time` could show a legend with RepleteCactus/Ottomans as red and Golden Horde as blue while the plotted paths remained hardcoded blue solid for `you` and red dashed for `opponent`. Fixed by threading `playerLabels` colors through the leader strip, allocation paths, age markers, significant-event markers, gather chart, and CSS player variables. *(source: conversation/code verification)*
- **[2026-05-02]** Webapp army value showed `5313` instead of AoE4World `1485` at `17:40` for RepleteCactus game `231277359`. Fixed in commit `46ea161` by using base unit cost for raw live army value, preserving destroyed-only upgraded rows, applying upgraded deaths across active unit-line tiers, and classifying religious units as military army value. *(source: conversation/code verification)*
- **[2026-05-02]** Local port `3000` had an older unresponsive `next-server`; fixed-route verification used a fresh dev server on `3001` and also found an older server on `3005` still serving stale `1185` output. Status: local environment cleanup may be needed outside the committed fix. *(source: conversation/code verification)*
- **[2026-05-01]** Yatai active count regressed to `9` when `9001316` bucket `15` was changed back to ignored. Fixed by restoring bucket `15` as destruction/removal and updating tests/audit expectations. *(source: conversation/code verification)*
- **[2026-05-01]** Focused tests briefly passed while live Yatai behavior was wrong because expectations had been changed to accept ignored Yatai bucket `15`; fixed by asserting Yatai bucket `15` produces `-125` deltas while Trade Cart bucket `15` remains ignored. *(source: conversation/code verification)*
- **[2026-05-01]** Vercel first production deployment failed with "No Output Directory named `public` found" because the project used static output defaults. Fixed by adding `apps/web/vercel.json`. *(source: conversation)*
- **[2026-05-01]** Running `npm --prefix apps/web run verify` concurrently with `npm --prefix apps/web run build` can race on `.next/types` and cause TS6053 missing-file errors. Fixed operationally by running them serially. *(source: conversation)*
- **[2026-05-01]** Allocation UI had text overlap/sprawl and too-small inspector/table text. Fixed with collapsed guide/secondary panels, larger type, wider chart area, narrower inspector, and mobile chart scrolling. *(source: conversation)*
- **[2026-05-01]** Vercel install reports 2 audit findings: 1 moderate and 1 high. Status: open/deferred. *(source: conversation)*
- **[2026-05-01]** Empty Vercel project `web` has no deployments. Status: left in place after user canceled deletion. *(source: conversation)*
- **[2026-05-01]** Same-timestamp production/destruction item deltas collapsed to zero because the aggregation key was `timestamp|band|itemKey`. Fixed in commit `8f3f932 Fix same-timestamp resource pool deltas`. *(source: conversation)*
- **[2026-05-01]** Gathered-series normalization is useful for smooth charts but misleading for exact timestamp ledger reconciliation. Status: open/deferred. *(source: conversation)*
- **[2026-05-01]** Full root/app verification is currently blocked by existing unknown-bucket/Yatai worktree changes, including failing `buildOrderResolver` expectations and unknown-bucket tests. Status: open/unrelated to same-timestamp fix. *(source: conversation)*

## Deferred Investigations

- **[2026-05-06]** If broader gather-rate drop detection is revisited later, it needs a way to distinguish opponent-caused disruption from normal player actions such as farm transitions, production-building construction, or resource reallocation. Current implementation intentionally avoids this by using only significant event windows. *(source: conversation)*
- **[2026-05-04]** Server-side `posthogAnalytics.ts` and the inline-client JS-as-string each implement `stripSensitiveQueryParams`, `shouldDropAnalyticsProperty`, and `sanitizeProperties` independently. Better to extract a shared rule list and assert both sides hold it via property tests over a shared corpus. Skipped this round to keep review-fix scope small; flagged in code comment. *(source: conversation/code review)*
- **[2026-05-04]** Decide whether sample-report clicks should continue linking directly to the match route or route through `/matches/open` so the loading-route instrumentation also captures sample-report flow. *(source: conversation/code verification)*
- **[2026-05-04]** Consider adding automated local PostHog smoke verification, or a documented MCP query snippet, so future analytics changes can prove ingestion and signature scrubbing without manual query reconstruction. *(source: conversation/code verification)*
- **[2026-05-03]** If post-deploy staleness in long-lived open tabs ever becomes user-visible, embed `VERCEL_GIT_COMMIT_SHA` as `<meta name="build-sha">` in the rendered HTML and add an inline script that compares it against a `/api/build-version` endpoint on `visibilitychange`, forcing a reload on mismatch. Considered overkill for current scale and not implemented. *(source: conversation)*
- **[2026-05-02]** Clarify labels around `allocation.military` versus raw `militaryActive`; the former can include broader military-category deployed value while the latter is the AoE4World-style live army value. *(source: conversation/code verification)*
- **[2026-05-01]** Consider adding a durable fixture derived from live match `229727104` so future unknown-bucket changes are checked against the real Yatai event sequence, not only synthetic timings. *(source: conversation)*
- **[2026-05-01]** Add web-app caching for AoE4World responses and static data. *(source: conversation)*
- **[2026-05-01]** Upgrade Next/PostCSS safely rather than using `npm audit fix --force` blindly. *(source: conversation)*
- **[2026-05-01]** Add a production-safe smoke test for a known public match route. *(source: conversation)*
- **[2026-05-01]** Clean up the empty `web` Vercel project if the user explicitly confirms deletion later. *(source: conversation)*
- **[2026-05-01]** Design the reconciliation UI so it is inspectable but clearly labeled as model/accounting diagnostic data. *(source: conversation)*
- **[2026-05-01]** Decide whether reconciliation should use raw AoE4World cumulative gathered arrays exclusively, or expose both raw and scaled values with labels. *(source: conversation)*
